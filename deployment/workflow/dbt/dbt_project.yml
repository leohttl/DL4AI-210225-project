name: 'dl4ai_stock_prediction'
version: '1.0.0'
config-version: 2

profile: 'dl4ai'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dl4ai_stock_prediction:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
